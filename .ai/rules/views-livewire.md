---
paths:
  - 'resources/views/livewire/**'
---

# Views Livewire

## Match the class-based convention
Implement application screens as class-based Livewire components with matching Blade views by default. Use `Route::livewire(...)` for full-page screens where appropriate.
